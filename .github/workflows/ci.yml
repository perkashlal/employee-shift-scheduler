name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Run unit tests (Surefire only)
        run: xvfb-run -a mvn -B -ntp clean test

<<<<<<< HEAD
      - name: Upload surefire reports
        if: always()
=======
      - if: always()
>>>>>>> 16871ff6c695f8ad7549b10f6372c5ec05eecc04
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: |
            **/target/surefire-reports/**
          if-no-files-found: warn

  integration-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: unit-tests

    env:
      DOCKER_API_VERSION: "1.44"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

<<<<<<< HEAD
      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb
=======
      - run: sudo apt-get update && sudo apt-get install -y xvfb
>>>>>>> 16871ff6c695f8ad7549b10f6372c5ec05eecc04

      - name: Docker info
        run: |
          docker version
          docker info

<<<<<<< HEAD
      # IMPORTANT:
      # -DskipTests skips unit tests (Surefire)
      # Failsafe integration tests still run during verify
      - name: Run integration + e2e tests (Failsafe only)
        run: xvfb-run -a mvn -B -ntp verify -DskipTests -DskipITs=false

      - name: Upload failsafe reports
        if: always()
=======
      - name: Run integration + e2e tests (Failsafe)
        run: xvfb-run -a mvn -B -ntp verify

      - if: always()
>>>>>>> 16871ff6c695f8ad7549b10f6372c5ec05eecc04
        uses: actions/upload-artifact@v4
        with:
          name: failsafe-reports
          path: |
            **/target/failsafe-reports/**
          if-no-files-found: warn

  coverage:
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: unit-tests

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

<<<<<<< HEAD
      # Runs unit tests + generates JaCoCo report, skipping IT/E2E
      - name: Run unit tests with JaCoCo (skip IT/E2E)
=======
      - name: Run unit tests with JaCoCo (skip IT/E2E in coverage job)
>>>>>>> 16871ff6c695f8ad7549b10f6372c5ec05eecc04
        run: xvfb-run -a mvn -B -ntp clean verify -Pjacoco -DskipITs

      - name: Upload JaCoCo report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: |
            **/target/site/jacoco/**
            **/target/site/jacoco-it/**
<<<<<<< HEAD
            **/target/site/jacoco/jacoco.xml
            **/target/site/jacoco-it/jacoco.xml
          if-no-files-found: warn
=======
          if-no-files-found: warn
>>>>>>> 16871ff6c695f8ad7549b10f6372c5ec05eecc04
